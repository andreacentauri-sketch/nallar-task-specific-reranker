# Portfolio Case Study — Model Tuning

I implemented a lightweight trainable reranker over the existing retrieval system. The project shows supervised parameter fitting, validation, reproducibility, and careful distinction between task-specific tuning and LLM weight fine-tuning.
